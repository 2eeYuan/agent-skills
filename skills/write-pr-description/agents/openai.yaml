interface:
  display_name: "PR Description"
  short_description: "Concise, evidence-backed, copyable PR descriptions"
  default_prompt: "Use $write-pr-description to draft a concise PR description from the final changes, using only applicable categories and one copyable Markdown block."
policy:
  allow_implicit_invocation: true
